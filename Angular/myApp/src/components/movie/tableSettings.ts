export interface ITableSettings{
    imageWidth:number;
    borderRadius:number;
    fontSize:number;
    color:string;
}

export let TableSettings:ITableSettings = {
    imageWidth: 60,
    borderRadius: 15,
    fontSize: 20,
    color:"#FFFF00"
}